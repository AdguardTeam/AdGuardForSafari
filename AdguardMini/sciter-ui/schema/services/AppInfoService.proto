syntax = "proto3";

import "PrimitivesWrappers.proto";
import "AppInfo.proto";

/* Service handles about app information */
service AppInfoService {
    /* Get About app info  */
    rpc GetAbout(EmptyValue) returns (AppInfo);
    /* Request to update the app */
    rpc UpdateApp(EmptyValue) returns (EmptyValue);
}
