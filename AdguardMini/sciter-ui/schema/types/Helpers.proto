syntax = "proto3";

option optimize_for = CODE_SIZE;

// Entity is used for transfer import/export path
message Path {
    string path = 1;
}

message OptionalError {
    bool hasError = 1;
    optional string message = 2;
}
